apiVersion: v1
kind: Service
metadata:
  name: {{ include "playlist-mirror.fullname" . }}
  labels:
    {{- include "playlist-mirror.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "playlist-mirror.selectorLabels" . | nindent 4 }}
